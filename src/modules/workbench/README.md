# 工作台

```
`workbench`
    ├── `approved` 我已审批
    ├── `initiate` 发起审批
    │   ├── `advance` 垫款(借款)申请
    │   ├── `invoicing` 开票申请
    │   ├── `load` 用款申请
    │   └── `refund` 退款申请
    ├── `mine` 我的审批
    └── `waiting` 待我审批
```
